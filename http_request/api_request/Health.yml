info:
  name: Health
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/health
  body:
    type: json
    data: |-
      {
        "prompt":"What is the capital of the France?"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
